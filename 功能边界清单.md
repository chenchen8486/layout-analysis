# 功能边界清单

> 生成时间：2026-07-07
> 项目：layout_analysis
> 工具：Pathfinder Phase 0

---

## 说明

本清单基于源码树、顶层文档和导入关系整理，覆盖当前已实现且处于活跃状态的功能。
P3（UI 界面）已按 `docs/optimization_plan.md` 决策**暂缓**，因此不纳入本清单。

---

## 功能列表

### 1. 配置加载与命令行解析
- **目的**：加载 YAML 配置（支持 `settings.local.yaml` 本地覆盖），解析命令行参数，按优先级合并配置。
- **入口点**：`main.py:62`、`main.py:333`、`main.py:399`
- **核心文件**：`main.py`、`config/settings.yaml`、`config/settings.local.yaml`
- **外部依赖**：PyYAML、python-dotenv、环境变量（`DEEPSEEK_API_KEY`、`MINERU_PATH`、`CONDA_PREFIX`、`HF_ENDPOINT`）

### 2. PDF 输入发现
- **目的**：从单文件或目录收集目标 PDF，可选递归子目录。
- **入口点**：`main.py:123`
- **核心文件**：`main.py`
- **外部依赖**：本地文件系统

### 3. MinerU CLI 执行
- **目的**：解析 MinerU 可执行文件位置（显式路径、conda 环境、环境变量、PATH），通过子进程调用 `mineru` 解析 PDF。
- **入口点**：`core/mineru_engine.py:14`、`core/mineru_engine.py:34`、`core/mineru_engine.py:120`
- **核心文件**：`core/mineru_engine.py`
- **外部依赖**：MinerU CLI 可执行文件、可选 Conda 环境

### 4. MinerU 输出目录推断
- **目的**：从生成结果中推断实际的 MinerU 输出子目录（默认 `<stem>/auto/`），并做兼容回退。
- **入口点**：`core/mineru_engine.py:90`
- **核心文件**：`core/mineru_engine.py`
- **外部依赖**：MinerU 输出目录布局

### 5. 版面分析解析
- **目的**：读取 MinerU 的 `content_list.json`，转换为类型化的 `LayoutElement`，并生成统计摘要（元素数量、页数、可翻译数量）。
- **入口点**：`core/layout_parser.py:53`、`core/layout_parser.py:66`、`core/layout_parser.py:132`
- **核心文件**：`core/layout_parser.py`
- **外部依赖**：MinerU `content_list.json` Schema

### 6. 流水线状态追踪 / 增量转换
- **目的**：为每个 PDF 维护持久化状态（`pipeline_state.json`），跳过已完成的阶段，并在源 PDF 变化时重新运行，支持断点续跑。
- **入口点**：`core/pipeline_tracker.py:48`、`core/pipeline_tracker.py:93`、`core/pipeline_tracker.py:128`
- **核心文件**：`core/pipeline_tracker.py`
- **外部依赖**：文件系统 mtime

### 7. DeepSeek Chat API 翻译
- **目的**：调用 DeepSeek Chat API，支持单文本、批量和 Markdown 全文翻译，具备重试、并发和分隔符批量机制。
- **入口点**：`core/translator.py:28`、`core/translator.py:71`、`core/translator.py:107`、`core/translator.py:157`、`core/translator.py:385`
- **核心文件**：`core/translator.py`
- **外部依赖**：DeepSeek API、`requests`

### 8. Markdown 分块与并发翻译
- **目的**：按字符限制将 Markdown 拆分为段落块，并发翻译，失败时回退原文。
- **入口点**：`core/translator.py:300`、`core/translator.py:330`
- **核心文件**：`core/translator.py`
- **外部依赖**：DeepSeek Chat API

### 9. 单 PDF 编排
- **目的**：协调单个 PDF 的 MinerU 解析、版面分析、可选翻译，写入翻译后的 Markdown 并记录阶段结果。
- **入口点**：`main.py:216`
- **核心文件**：`main.py`
- **外部依赖**：`MinerUEngine`、`LayoutParser`、`PipelineTracker`、`DeepSeekTranslator`

### 10. 批量运行编排与报告
- **目的**：遍历收集到的 PDF，累计成功/跳过/失败统计，写入 `batch_summary.json` 并输出终端摘要。
- **入口点**：`main.py:399`、`main.py:549`
- **核心文件**：`main.py`
- **外部依赖**：所有单 PDF 阶段

### 11. 日志基础设施
- **目的**：提供文件 + 控制台日志，使用 UTF-8-sig 编码，支持级别配置和 reset-safe 的 Handler 管理。
- **入口点**：`utils/logger.py:9`
- **核心文件**：`utils/logger.py`
- **外部依赖**：Python 标准 `logging`

---

## 已知缺口

- 缺少针对真实 MinerU / DeepSeek 的集成测试，当前测试依赖 mock 与本地 fixture。
- MinerU 失败时缺少流水线级重试，仅 DeepSeek API 请求有重试。
- `settings.local.yaml` 中硬编码了本地路径，跨环境需要手动覆盖。

## 已修复

- ✅ `_build_markdown_lines` 在 `main.py` 中定义但当前流程未使用 → 已删除该未使用函数。
